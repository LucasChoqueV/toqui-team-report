version: '3.4'

networks:
  global:
services:
  team-report-app:
    container_name: team-report-app-container
    build:
      context: .
    networks:
      - global
    ports:
      - 3000:80
    environment:
      - TOQUI_JIRA_REPORT_API=http://localhost:5555/api/report